---
title: "Einführung in das SQL ORDER BY Schlüsselwort"
schema: "/databases/nordwind.sql"
pdf: "/databases/pdf/NordwindDB.pdf"
difficulty: "medium"
number: 3
query: |
  SELECT COUNT("CustomerID") AS "Number of Customers", 
    "Country"
  FROM customers 
  GROUP BY "Country" 
  ORDER BY COUNT("CustomerID") DESC;
---

## Einführung in die SQL Schlüsselwörter ORDER BY und GROUP BY

### `ORDER BY`

Das SQL `ORDER BY` Schlüsselwort wird verwendet, um die Ergebnisse einer Abfrage in einer bestimmten Reihenfolge zu sortieren. Dies ermöglicht es, Daten geordnet nach einer oder mehreren Spalten abzurufen, wodurch die Analyse und Interpretation der Daten erleichtert wird.

#### Verwendung des ORDER BY Schlüsselworts

Die grundlegende Syntax für die Verwendung von `ORDER BY` lautet:

```sql
SELECT spalte1, spalte2, ... FROM tabelle ORDER BY spalte [ASC|DESC];
```

- `spalte1, spalte2, ...`: Die Spalten, die in der Ergebnismenge enthalten sein sollen.
- `tabelle`: Der Name der Tabelle, aus der die Daten abgerufen werden.
- `ORDER BY spalte`: Die Spalte, nach der die Daten sortiert werden sollen.
- `[ASC|DESC]`: Optional; gibt die Sortierreihenfolge an. `ASC` steht für aufsteigende Reihenfolge (Standard) und `DESC` für absteigende Reihenfolge.

Das `ORDER BY` Schlüsselwort ist ein mächtiges Werkzeug in SQL, das es ermöglicht, Daten geordnet und strukturiert abzurufen, was die Handhabung und Interpretation der Daten erheblich vereinfacht.

Weiterführende Informationen finden Sie hier: [w3schools.com](https://www.w3schools.com/sql/sql_orderby.asp)

### `GROUP BY`

Das SQL `GROUP BY` Schlüsselwort wird verwendet, um Zeilen mit identischen Werten in bestimmten Spalten zu gruppieren. Dies ermöglicht es, Aggregatfunktionen (wie `COUNT`, `SUM`, `AVG`) auf jede Gruppe anzuwenden, um Zusammenfassungen oder Statistiken zu erstellen. Dadurch lassen sich Daten strukturiert auswerten, z. B. um Muster oder Gesamtwerte pro Kategorie zu ermitteln.

**Beispiel**:  
```sql
SELECT spalte1, COUNT(*) AS anzahl
FROM tabelle
GROUP BY spalte1;
```  
Diese Abfrage gruppiert nach dem Inhalt von `spalte1` und zählt, wie viele Zeilen jeweils mit dem Inhalt von `spalte1` vorhanden sind.

Weiterführende Informationen finden Sie hier: [w3schools.com](https://www.w3schools.com/sql/sql_groupby.asp)

---

## Woher kommen die MEISTEN Kunden?

### Aufgabenstellung

 Wo kommen die meisten Kunden her?  

#### Ausgabe

| Number of Customers | Country     |
|--------------------:|-------------|
| 13                  | USA         |
| 11                  | Germany     |
| 11                  | France      |
| 9                   | Brazil      |
| 7                   | UK          |
| 5                   | Spain       |
| 5                   | Mexico      |
| 4                   | Venezuela   |
| 3                   | Italy       |
| 3                   | Canada      |
| 3                   | Argentina   |
| 2                   | Austria     |
| 2                   | Switzerland |
| 2                   | Denmark     |
| 2                   | Belgium     |
| 2                   | Portugal    |
| 2                   | Finland     |
| 2                   | Sweden      |
| 1                   | Norway      |
| 1                   | Poland      |
| 1                   | Ireland     |