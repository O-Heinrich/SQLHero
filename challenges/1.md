---
title: "Woher kommen die Kunden?"
schema: "/databases/nordwind.sql"
number: 1
difficulty: "easy"
query: |
  SELECT DISTINCT 
    "Country" 
  FROM 
    customers 
  ORDER BY 
    "Country" 
  ASC;
---

## Einführung in SQL SELECT und SELECT DISTINCT Statements

SQL (Structured Query Language) ist eine weit verbreitete Sprache zur Verwaltung und Manipulation von Daten in relationalen Datenbanken. Ein grundlegendes SQL-Kommando ist das `SELECT` Statement, das verwendet wird, um Daten aus einer oder mehreren Tabellen abzurufen.

#### Das SELECT Statement

Das `SELECT` Statement ermöglicht es, bestimmte Datenfelder aus einer Tabelle zu extrahieren. Hier ist ein einfaches Beispiel:

```sql
SELECT spalte1, spalte2 FROM tabelle;
```

In diesem Beispiel werden die Spalten `spalte1` und `spalte2` aus der Tabelle `tabelle` ausgewählt. Wenn alle Spalten der Tabelle ausgewählt werden sollen, kann das Sternchen (`*`) verwendet werden:

```sql
SELECT * FROM tabelle;
```

#### Hinweis zur SQL-Syntax in SQL Hero

SQL Hero verwendet eine WASM-kompilierte PostgreSQL-Version zur Auswertung Ihrer Queries. Beachten Sie folgende Syntaxbesonderheiten:

1. **Stringliterale**  
   - Immer mit einfachen Anführungszeichen definieren:  
     ```sql
     'Ich bin ein String'
     ```

2. **Feldnamen**  
   - Ohne Anführungszeichen: Automatische Konvertierung zu Lowercase  
     ```sql
     Country → wird zu "country"
     ```
   - Mit doppelten Anführungszeichen: Beibehaltung der exakten Schreibweise  
     ```sql
     "Country" → bleibt "Country"
     ```

**Beispiele:**  
❌ Falsch (ungewollte Lowercase-Konvertierung):
```sql
SELECT Country FROM customers;  -- Wird zu: SELECT country FROM customers;
```

✅ Korrekt (explizite Großschreibung):
```sql
SELECT "Country" FROM customers;  -- Bleibt: SELECT "Country" FROM customers;
```


#### Das SELECT DISTINCT Statement

Manchmal enthalten die Daten, die aus einer Tabelle abgerufen werden, Duplikate. Um nur eindeutige Werte zu erhalten, kann das `SELECT DISTINCT` Statement verwendet werden. Dieses Kommando entfernt Duplikate und gibt nur einzigartige Datensätze zurück.

Weiterführende Informationen finden Sie hier:  [w3schools.com](https://www.w3schools.com/sql/sql_distinct.asp)

---

## Woher kommen die Kunden?

### Aufgabenstellung

Aus welchen Ländern kommen die Kunden? 

#### Ausgabe

| Länder | |
|--------|-|
| Argentina | |
| Austria | |
| Belgium | |
| Brazil | |
| Canada | |
| Denmark | |
| Finland | |
| France | |
| Germany | |
| Ireland | |
| Italy | |
| Mexico | |
| Norway | |
| Poland | |
| Portugal |
| Spain | |
| Sweden | |
| Switzerland | |
| UK | |
| USA | |
| Venezuela | |